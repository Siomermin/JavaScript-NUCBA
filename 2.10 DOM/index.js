// *Crear un archivo HTML que contenga:
// -H1.
// -p.
// -div vacío.
// -inputs de tipo text, password, email, number, checkbox.

// *Asignar un id a cada elemento.
// *Capturar cada elemento en una variable.
// *Hacer un console.dir() de cada elemento capturado y revisar en el navegador qué devuelve y 
// que cambia cuando modificamos los elementos (Checkbox tildado o destildado, inputs vacíos o con contenido).

const h1 = document.getElementById("h1");
const p = document.getElementById("p");
const div = document.getElementById("div");
const text = document.getElementById("text");
const password = document.getElementById("password");
const email = document.getElementById("email");
const number = document.getElementById("number");
const checkbox = document.getElementById("checkbox");

console.dir(h1);
console.dir(p);
console.dir(div);
console.dir(text);
console.dir(password);
console.dir(email);
console.dir(number);
console.dir(checkbox);

